quarkus:
  datasource:
    jdbc:
      url: jdbc:postgresql://database:5432/isolation
    db-kind: postgresql
    username: postgres
    password: password
#    host: 0.0.0.0
  #  cors:
   #  ~: true
   #   origins: http://localhost:3000,http://localhost:9090
#      headers: accept, authorization, content-type, x-requested-with
#      methods: GET, POST, PUT, DELETE, OPTIONS, HEAD